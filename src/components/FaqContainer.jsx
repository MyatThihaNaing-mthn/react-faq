function FaqContainer({children}){
    return (
        <div className="w-full h-full flex items-center justify-center flex-col xs:px-10">
            {children}
        </div>
    );
}

export default FaqContainer;